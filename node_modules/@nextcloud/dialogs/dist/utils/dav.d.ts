import { ContentsWithRoot, Node } from '@nextcloud/files';
import { WebDAVClient } from 'webdav';
import { CancelablePromise } from 'cancelable-promise';
/**
 * Get the recently changed nodes from the last two weeks
 * @param client The WebDAV client
 */
export declare function getRecentNodes(client: WebDAVClient): CancelablePromise<Node[]>;
/**
 * Get the directory content
 * @param client The WebDAV client
 * @param directoryPath The path to fetch
 */
export declare function getNodes(client: WebDAVClient, directoryPath: string): CancelablePromise<ContentsWithRoot>;
/**
 * Get information for one file
 *
 * @param client The WebDAV client
 * @param path The path of the file or folder
 */
export declare function getFile(client: WebDAVClient, path: string): Promise<Node>;
