import { Folder, Node } from '@nextcloud/files';
import { ComputedRef, Ref } from 'vue';
/**
 * Handle file loading using WebDAV
 *
 * @param currentView Reference to the current files view
 * @param currentPath Reference to the current files path
 */
export declare const useDAVFiles: (currentView: Ref<"files" | "recent" | "favorites"> | ComputedRef<"files" | "recent" | "favorites">, currentPath: Ref<string> | ComputedRef<string>) => {
    isLoading: Ref<boolean>;
    files: Ref<Node[]>;
    folder: Ref<Folder | null> | import('vue').ShallowRef<Folder | null>;
    loadFiles: () => Promise<void>;
    createDirectory: (name: string) => Promise<Folder>;
};
